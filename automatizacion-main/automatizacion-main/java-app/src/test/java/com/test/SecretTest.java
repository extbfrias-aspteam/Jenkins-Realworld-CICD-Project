package com.test;

public class SecretTest {
    // 1. Simular un Token de AWS (Usa el prefijo oficial AKIA que busca SonarQube)
    private static final String AWS_KEY = ""; 
    private static final String AWS_SECRET = ""
    // 2. Asignación explícita con alta entropía (texto largo y complejo)
    public void connectDatabase() {
        String db_password = ""; // Contraseña compleja hardcodeada
        String apiKey = ""; // Patrón de Token de GitHub
    }
}